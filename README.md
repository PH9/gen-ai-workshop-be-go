# Gin-Gonic Go Project

## How to run

```fish
# Run the server
 go run main.go
```

- The server will start on http://localhost:8080
- Test the API: `GET /` should return `{ "message": "Hello World" }`

## Reference
- Gin-Gonic documentation: https://gin-gonic.com/
